# `common` package

This package contains shared types and type utilities.
It's intended for low-level primitives that are shared across multiple other modules and have no single owner.

## Modules

- `common/src/`
  - `types/`: Shared type definitions
  - `utils/`: Shared utility functions
