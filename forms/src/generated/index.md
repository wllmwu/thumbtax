# `generated` module

This module contains the generated form specification objects.
Each form specification is a const export from the TypeScript file named after its FormClass identifier (`f1040.ts`, `fW2.ts`, etc.).
There is no business logic in this module, only static objects.
